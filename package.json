{
  "name": "jquery-cover.js",
  "version": "0.1.0",
  "description": "Fix the size of content to the container as large as possible, keeping aspect ratio.",
  "main": "dist/jquery-cover.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mach3 <http://github.com/mach3>",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-concat": "^0.5.1",
    "grunt-contrib-uglify": "^0.9.1",
    "load-grunt-tasks": "^3.2.0"
  }
}
